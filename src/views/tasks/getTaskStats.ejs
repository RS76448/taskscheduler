<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Task Stats - My App</title>
<style>
  /* General Page Styling */
body {
  font-family: Arial, sans-serif;
  background-color: #f4f4f4;
  margin: 0;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
}

.container {
  background: #fff;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
  width: 90%;
  max-width: 800px;
}

/* Headings */
h1 {
  text-align: center;
  color: #333;
}

h2 {
  margin-top: 20px;
  color: #555;
  border-bottom: 2px solid #ddd;
  padding-bottom: 5px;
}

/* Table Styling */
table {
  width: 100%;
  border-collapse: collapse;
  margin-top: 10px;
}

table th, table td {
  border: 1px solid #ddd;
  padding: 10px;
  text-align: left;
}

table th {
  background-color: #007bff;
  color: white;
}

/* Task Status Color Coding */
td:first-child {
  font-weight: bold;
}

td:nth-child(2) {
  text-align: center;
}

td:first-child::before {
  content: "⬤ ";
  font-size: 14px;
}

td:contains("waiting")::before {
  color: orange;
}

td:contains("active")::before {
  color: blue;
}

td:contains("completed")::before {
  color: green;
}

td:contains("failed")::before {
  color: red;
}

/* Auth Message */
#auth-message {
  text-align: center;
  margin-bottom: 15px;
  color: red;
  font-weight: bold;
}

</style>
</head>
<body>
  <div class="container">
    <h1>Task Statistics</h1>
    
    <div id="auth-message"></div> <!-- Placeholder for login message -->

    <div class="stats">
      <h2>Task Stats by Status</h2>
      <table>
        <thead>
          <tr>
            <th>Status</th>
            <th>Count</th>
          </tr>
        </thead>
        <tbody id="task-stats-body">
          <!-- Data will be inserted here -->
        </tbody>
      </table>

      <h2>Queue Stats</h2>
      <p><strong>Waiting Tasks:</strong> <span id="waiting-tasks">Loading...</span></p>
      <p><strong>Active Tasks:</strong> <span id="active-tasks">Loading...</span></p>
      <p><strong>Completed Tasks:</strong> <span id="completed-tasks">Loading...</span></p>
      <p><strong>Failed Tasks:</strong> <span id="failed-tasks">Loading...</span></p>
      <p><strong>Total Tasks:</strong> <span id="total-tasks">Loading...</span></p>
    </div>
  </div>

  <script src="/js/gettaskstatus.js"></script>
</body>
</html>
